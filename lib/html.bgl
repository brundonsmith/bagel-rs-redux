export type HTML = {
    tag: 'h1' | 'p' | 'a',
    attributes: {},
    foo,
    bar
    children: unknown[],
    blah
}

const renderHTML = (html: HTML) => 'ddd' + html.tag

const foo = 'aaa' + html.tag

export const foo = 12 + 2