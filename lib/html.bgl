export type HTML = {
    tag: 'h1' | 'p' | 'a',
    attributes: {},
    children: unknown[],
}

const renderHTML = (html: HTML) => '<' + html.tag + '>' + html.children.join('') + '</' + html.tag + '>'

const foo = 'aaa' + html.tag

export const foo = 12 + 2